<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

    <conversionRule conversionWord="clr"
      converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
      converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx"
      converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>

    <property name="LOG_DIR" value="./logs"/>

    <property name="CONSOLE_LOG_PATTERN"
      value="[%d{yyyy-MM-dd HH:mm:ss}:%-3relative] %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:-}){magenta} %clr(---){faint} %clr([%15.15thread]){faint} %clr(%-40.40logger{36}){cyan} %clr(:){faint} %msg%n"/>
    <property name="CONSOLE_LOG_CHARSET" value="${CONSOLE_LOG_CHARSET:-default}"/>
    <property name="FILE_LOG_PATTERN"
      value="[%d{yyyy-MM-dd HH:mm:ss}:%-3relative] ${LOG_LEVEL_PATTERN:-%5p} ${PID:-} --- [%15.15thread] %-40.40logger{36} : %msg%n"/>

    <springProperty scope="context" name="SERVICE_NAME" source="spring.application.name"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="io.pinkspider.global.util.MaskingPatternLayout">
                <maskPattern>\"ssn\"\s*:\s*\"(.*?)\"</maskPattern>
                <maskPattern>ssn\s*=\s*([^,)\s]+)</maskPattern>
                <maskPattern>\"ssn_birth\\{0,1}\"\s*:\s*\\{0,1}\"(.*?)\"</maskPattern>
                <maskPattern>ssnBirth\s*=\s*([^,)\s]+)</maskPattern>
                <maskPattern>\"full_name\\{0,1}\"\s*:\s*\\{0,1}\"(.*?)\"</maskPattern>
                <maskPattern>fullName\s*=\s*([^,)\s]+)</maskPattern>
                <maskPattern>\"telecom\\{0,1}\"\s*:\s*\\{0,1}\"(.*?)\"</maskPattern>
                <maskPattern>telecom\s*=\s*([^,)\s]+)</maskPattern>
                <maskPattern>\"phone_number\\{0,1}\"\s*:\s*\\{0,1}\"(.*?)\"</maskPattern>
                <maskPattern>phoneNumber\s*=\s*([^,)\s]+)</maskPattern>
                <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            </layout>
            <charset>${CONSOLE_LOG_CHARSET}</charset>
        </encoder>
    </appender>

    <appender name="dailyRollingFileAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${SERVICE_NAME}-${HOSTNAME}.log</file>
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="io.pinkspider.global.util.MaskingPatternLayout">
                <maskPattern>\"ssn\"\s*:\s*\"(.*?)\"</maskPattern>
                <maskPattern>ssn\s*=\s*([^,)\s]+)</maskPattern>
                <maskPattern>\"ssn_birth\\{0,1}\"\s*:\s*\\{0,1}\"(.*?)\"</maskPattern>
                <maskPattern>ssnBirth\s*=\s*([^,)\s]+)</maskPattern>
                <maskPattern>\"full_name\\{0,1}\"\s*:\s*\\{0,1}\"(.*?)\"</maskPattern>
                <maskPattern>fullName\s*=\s*([^,)\s]+)</maskPattern>
                <maskPattern>\"telecom\\{0,1}\"\s*:\s*\\{0,1}\"(.*?)\"</maskPattern>
                <maskPattern>telecom\s*=\s*([^,)\s]+)</maskPattern>
                <maskPattern>\"phone_number\\{0,1}\"\s*:\s*\\{0,1}\"(.*?)\"</maskPattern>
                <maskPattern>phoneNumber\s*=\s*([^,)\s]+)</maskPattern>
                <pattern>${FILE_LOG_PATTERN}</pattern>
            </layout>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/${SERVICE_NAME}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <logger name="io.pinkspider" additivity="false" level="INFO">
        <appender-ref ref="dailyRollingFileAppender"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <logger name="org.springframework" additivity="false" level="INFO">
        <appender-ref ref="dailyRollingFileAppender"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <logger name="org.springframework.web.filter.CommonsRequestLoggingFilter" level="INFO">
        <appender-ref ref="dailyRollingFileAppender"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <logger name="jdbc" level="OFF"/>
    <logger name="jdbc.sqlonly" level="OFF"/>
    <logger name="jdbc.sqltiming" level="DEBUG"/>
    <logger name="jdbc.audit" level="OFF"/>
    <logger name="jdbc.resultset" level="OFF"/>
    <logger name="jdbc.resultsettable" level="DEBUG"/>
    <logger name="jdbc.connection" level="OFF"/>

    <root level="INFO">
        <appender-ref ref="dailyRollingFileAppender"/>
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>

