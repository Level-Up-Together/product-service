package io.pinkspider.leveluptogethermvp.userservice.core.extension;

public class UserQueryDslExtension {
/*

    @QueryDelegate(User.class)
    public static BooleanBuilder getConditionUserToNotActivityMode(QUser user, List<String> activityModes) {
        BooleanBuilder builder = new BooleanBuilder();
        if (activityModes != null && activityModes.size() != 0) {
            builder.and(user.activityMode.notIn(activityModes));
        }
        return builder;
    }

    @QueryDelegate(User.class)
    public static BooleanBuilder getConditionUserToName(QUser user, String name) {
        BooleanBuilder builder = new BooleanBuilder();
        if (StringUtils.isNotBlank(name)) {
            builder.and(user.firstName.eq(name));
            builder.or(user.fullName.eq(name));
        }
        return builder;
    }

    @QueryDelegate(User.class)
    public static BooleanBuilder getConditionUserToMobileNumber(QUser user, String mobileNumber) {
        BooleanBuilder builder = new BooleanBuilder();
        if (StringUtils.isNotBlank(mobileNumber)) {
            builder.and(user.mobileNumber.eq(mobileNumber));
        }
        return builder;
    }

    @QueryDelegate(User.class)
    public static BooleanBuilder getConditionUserToDidAllowMarketing(QUser user, Boolean didAllowMarketing) {
        BooleanBuilder builder = new BooleanBuilder();
        if (didAllowMarketing != null) {
            builder.and(user.didAllowMarketing.eq(didAllowMarketing));
        }
        return builder;
    }
*/

}
